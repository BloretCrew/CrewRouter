# Showcase 与 i18n 审查结果

审查对象：提交 `a11b399` 相对父提交的 diff。审查范围仅包括 `public/pages/showcase.html`、`lang/zh.json`、`lang/en.json`。

## Issues

- **severity: high**
  - **file:line:** `lang/zh.json:2691`
  - **description:** 新增标题 key `插件系统（含主题）` 的 zh value 不是 key 本身，而是功能描述 `沙箱插件支持网关钩子、页面/插槽/路由扩展，并可接入多套主题。`。这违反 zh.json 的 identity 约定；中文环境渲染该功能卡片标题时会显示整段描述，而不是“插件系统（含主题）”。
  - **suggestion:** 将该项改为 `"插件系统（含主题）": "插件系统（含主题）"`，功能描述继续使用独立的描述 key。
  - **status:** confirmed

- **severity: minor**
  - **file:line:** `public/pages/showcase.html:547-548`（对应 `lang/zh.json:2699`、`lang/en.json:2708`）
  - **description:** 插件商店卡片覆盖了浏览、评分、评论、一键安装、每日更新检测和自托管源，但未标明任务书要求的 demo 模式；英文译文也未体现 demo 模式。该信息对介绍页用户判断功能可用范围有一定影响。
  - **suggestion:** 在中英文描述中补充“demo 模式”并保留自托管源说明，例如中文加入“（demo 模式）”，英文加入“(demo mode)”。
  - **status:** confirmed

## Requirements check

- 7 个新增功能卡片均存在，旧的 9 个卡片保留，合计 16 个；新增卡片标题分别为插件系统（含主题）、插件商店、注入提示词、全站国际化、客户端事件上报 / 实时看板、OAuth 授权服务、模型库增强（收藏/搜索）。
- `showcase.html` 的新增内容符合既有 `<h3><span data-i18n>` 与 `<p><span data-i18n>` 卡片结构；该提交在指定文件中未改动样式或脚本，仅增加卡片结构与文案。
- 新增 HTML 文案对应的 key 在 zh/en 均存在；HTML 全部 86 个唯一 `data-i18n` key 均可在两份语言文件中找到，缺失均为 0。
- 本次提交向 zh/en 各新增 12 个 JSON key，集合完全对齐；en 新增 value 均非空且语义准确。zh 新增 value 仅 `插件系统（含主题）` 不符合 identity，已在上方列为问题；其余新增 zh value 均为 identity。
- `git diff --check` 通过。

结论：存在 2 个问题，不能判定为完全符合任务书；其中 zh identity 错误会直接导致中文标题显示异常，应优先修复。
