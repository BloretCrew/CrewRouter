# Blora Admin 复审结果

基线：`0f9528c`  
范围：当前 worktree 相对基线的改动，以及任务书 `/data/CrewRouter/.hermes/plans/2026-08-28_blora_admin_task.md` 要求的 admin/console/playground 收敛项。

## 检查结论

- `admin.html` 当前包含 20 个 `<blora-dialog>`，开闭标签数量均为 20；与基线中的 20 个 Modal ID 一一对应。
- `admin.html` 静态源码中未发现 `class="btn ..."`、`modal-header`、`modal-body`、`modal-footer`、`<select>`；静态 input/textarea 均已带 `.blora-input`/`.blora-textarea`。
- `node --check public/js/admin.js public/js/app.js public/js/playground.js` 通过。
- `git diff --check 0f9528c -- ...` 通过。
- 任务书指定的三个回归脚本均通过，共报告 17 个断言及 request-source 断言通过。
- 版本号已按当前基线递增：`admin.js?v=17`、`app.js?v=56`、`playground.js?v=3`。

## Issues

### Issue 1 -- Severity: bug

- **File:** `/data/CrewRouter/public/pages/admin.html:2212-2284`、`/data/CrewRouter/public/js/admin.js:6004-6021`
- **Description:** `providerQuotaModal` 迁移为 `<blora-dialog>` 时删除了 `quotaContent` 和 `quotaScriptEditor` 初始的 `display:none`，但 JS 仍假定它们可通过显示状态控制：查询开始时分别执行 `content.style.display = 'none'`、`editor.style.display = 'none'`。因此在 Blora 组件首次打开前，弹窗的结果区和脚本编辑器会作为默认 body 内容同时可见；这也改变了旧 Modal 的初始行为。
- **Suggestion:** 恢复这两个区域的初始隐藏状态，或在组件模板中采用等价的状态属性/类，并确保查询成功、编辑脚本、取消编辑各状态只显示对应区域。
- **Status: open**

### Issue 2 -- Severity: bug

- **File:** `/data/CrewRouter/public/js/admin.js:3672,3868,3992,4068,4959,9500,9760,9829,10153,10447,10937,10994`
- **Description:** admin.js 仍大量调用 `Dialog.showModal()`，继续依赖 `/js/dialog.js?v=3` 的自建 overlay/dialog 系统；其动态内容还继续生成 `dialog-btn`、`btn`、`input`、`select` 等传统控件。任务书要求 24 个后台弹窗统一使用 Blora API/结构，并删除对应自建 modal CSS/专属旧 JS；当前静态 20 个弹窗虽已迁移，但用户组、Team、批量同步、AI 错误处理等路径仍会打开第二套旧弹窗，形成半迁移态，样式、关闭行为和无障碍语义不一致。
- **Suggestion:** 将这些动态弹窗改为预置的 `<blora-dialog>` 或统一的 Blora dialog 创建/填充方式；动态按钮、输入和选择器使用 `.blora-button`、`.blora-input`/`.blora-select`，完成后移除 admin 页面对此 `dialog.js` 的依赖。
- **Status: open**

### Issue 3 -- Severity: bug

- **File:** `/data/CrewRouter/public/js/admin.js:8449-8460`
- **Description:** `renderModelListTags()` 仍直接执行 `container.innerHTML = modelList.map(...)`。虽然模型名经过 `escapeHtml`，但这违反项目 DOM 安全约束（业务代码不得直接写 `innerHTML`），并绕过了 `setHTML`/安全模板入口；后续维护者很容易在同一处加入未转义字段。
- **Suggestion:** 使用 `setHTML(container, html`...`)`，对动态模型名继续用默认插值转义；或使用 `clearChildren()`/DOM 节点 API 构造标签。
- **Status: open**

### Issue 4 -- Severity: bug

- **File:** `/data/CrewRouter/public/js/app.js:850-863`
- **Description:** console 遗留的 `showApiKeyRouteQueueTip()` 仍直接执行 `tip.innerHTML = ...`，并把队列中的模型名称写入 title/text 内容。当前名称虽调用了 `escapeHtml`，但这仍是任务书要求清理的直接 HTML 写入路径，且与 `dom.js` 明确规定的唯一 HTML 写入口冲突。
- **Suggestion:** 改用 `setHTML` 配合安全 `html` 模板，或完全使用 DOM API；所有动态名称、title 和标签均保持转义。
- **Status: open**

### Issue 5 -- Severity: bug

- **File:** `/data/CrewRouter/public/js/app.js:8296`、`/data/CrewRouter/public/js/app.js:8265`、`/data/CrewRouter/public/js/app.js:6594,6679`
- **Description:** console 仍有多处直接 `innerHTML` 写入（跟踪报告详情、跟踪报告列表、任务通知条、总结内容等）。其中任务条包含 `onclick` 字符串和服务端报告字段，属于本任务要求的 console 遗留安全收敛范围；即便部分字段已 escape，也没有统一经过安全 DOM 写入入口，且裸 `btn`/内联事件仍会继续产生传统控件。
- **Suggestion:** 统一改为 `setHTML`/`html` 安全模板或 DOM API；动态操作改为事件委托和 `data-*`，动态内容与属性分别使用 `textContent`/`setAttribute`，不要拼接 inline `onclick`。
- **Status: open**

### Issue 6 -- Severity: suggestion

- **File:** `/data/CrewRouter/public/pages/playground.html:29,107,111,119,137,183-189`、`/data/CrewRouter/public/js/playground.js:49-89,1290`
- **Description:** playground 仍保留原生 `<select>`、裸 `input/textarea`、旧 `.btn/.select/.input/.textarea` 标记及自建历史弹窗结构；虽然 `dom.js` 的运行时增强会尝试转换部分控件，静态页面仍不是 Blora 一致结构，且 `renderHistoryModal()` 仍以 `modal.style.display = 'flex'` 打开弹窗。任务书明确将 playground 版本更新与遗留收敛列入本任务验证范围。
- **Suggestion:** 在 HTML 中直接使用 `<blora-select>`、`.blora-input`/`.blora-textarea` 和 `<blora-dialog>`，并改用统一 `show()`/`close()`；动态 option 也通过 `blora-option` 或安全的统一增强入口生成，避免依赖运行时兜底转换。
- **Status: open**

### Issue 7 -- Severity: suggestion

- **File:** `/data/CrewRouter/public/js/admin.js:4942-4967,9500-11000`、`/data/CrewRouter/public/js/admin.js:823-830,1569-1574,2269-2271`
- **Description:** 除 Issue 2 所述动态 Dialog 外，admin.js 动态渲染的后台操作列表仍大量输出传统 `class="btn ..."`、原生 `select`/`input`；`dom.js` 只在组件增强流程中兜底追加/转换，不能满足任务书“181 按钮、54 select、103 input/textarea 全量 Blora 化”的源码契约，也使动态内容与已迁移的 admin.html 静态内容产生不同样式和行为。
- **Suggestion:** 按动态渲染路径逐处改为 Blora 标记或使用统一安全渲染后的 `upgradeBloraControls()`，并补充对动态节点的行为验证；优先处理用户组、Team、供应商向导和 AI 错误弹窗路径。
- **Status: open**

## 空 Issues 结论

非空：上述 7 个问题均保持 `Status: open`。因此本次复审不能判定任务书要求已全部完成。
