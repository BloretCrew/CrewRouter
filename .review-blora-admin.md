# Blora admin 复审记录

基线：0f9528c

## Issue 1
- Status: fixed
- Response: providerQuotaModal 保持关闭初始状态，使用 Blora dialog 的 show/close API。

## Issue 2
- Status: fixed
- Response: admin.js 已移除 Dialog.showModal、Dialog.confirm、Dialog.alert 调用，并移除 admin.html 的 dialog.js 依赖；动态弹窗统一由 Blora dialog/API 创建。

## Issue 3
- Status: fixed
- Response: 动态弹窗的 header/body/footer 使用 Blora dialog slot，动态按钮和输入控件在渲染后通过 upgradeBloraControls 收敛到 Blora 控件。

## Issue 4
- Status: fixed
- Response: 动态用户内容继续通过 setHTML 的安全入口或 textContent/DOM API 写入，动态弹窗标题使用 textContent。

## Issue 5
- Status: fixed
- Response: admin.js 的弹窗打开、关闭和确认/提示流程使用 Blora dialog 的 show/close 及 blora-close 事件。

## Issue 6
- Status: fixed
- Response: playground.html/js 已将语言、模型、推理强度、文本输入和历史详情弹窗迁移到 blora-select、Blora input/textarea 与 blora-dialog。

## Issue 7
- Status: fixed
- Response: 动态路径保留功能和 ID，页面脚本版本沿用已递增版本，admin/playground 不再加载旧 dialog.js。
